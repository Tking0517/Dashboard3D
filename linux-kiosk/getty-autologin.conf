# Installed to /etc/systemd/system/getty@tty1.service.d/override.conf
# Logs the appliance user straight in on tty1 with no password prompt.
# @APPLIANCE_USER@ is substituted by install.sh.
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin @APPLIANCE_USER@ --noclear %I $TERM
